let Order : string = "Pizza";

switch(Order) {
    case "Pizza":
        console.log(`Pizza will be prepared`);
        break;
    case "Burger":
        console.log(`Burger will be prepared`);
        break;
}